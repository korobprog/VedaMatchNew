{
  "name": "vedamatch",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:web-app-parity": "node --test scripts/web-app-parity/*.test.mjs",
    "postinstall": "pnpm --filter @vedamatch/shared build || true"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
