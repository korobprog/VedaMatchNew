/**
 * Токен внедрения провайдера эфемерид. Через него ведический слой получает
 * астрономию, не зная о конкретной реализации, — иначе смена движка потянула бы
 * за собой правки во всех потребителях.
 */
export const EPHEMERIS_PROVIDER = Symbol('EPHEMERIS_PROVIDER');
