-- Линия записи Музыки больше не ставится сама.
--
-- Умолчанием был ISKCON, а заливка вдобавок писала линию загрузившего:
-- бхаджан получал линию человека, а не записи, и пропадал из каталога у
-- преданных других линий. Теперь `null` — «для всех линий», и линию
-- проставляет редактор осознанно.
--
-- Уже записанные значения не трогаем: под ними могут быть и правильные,
-- проставленные руками. Разобрать их можно только глазами, из админки.
ALTER TABLE "public"."MusicTrack" ALTER COLUMN "lineage" DROP DEFAULT;

ALTER TABLE "public"."MusicIngestBatch" ALTER COLUMN "lineage" DROP DEFAULT;
