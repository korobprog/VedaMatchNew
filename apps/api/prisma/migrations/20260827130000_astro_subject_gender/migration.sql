-- Пол записи. Нужен гана-куте: её традиционная таблица асимметрична, и без
-- пола расчёт берёт более благоприятное из двух направлений — счёт выходит
-- завышенным. NULL остаётся допустимым: пол известен не всегда, и заставлять
-- выдумывать его хуже, чем честно оговорить неточность в ответе.
ALTER TABLE "AstroSubject" ADD COLUMN "gender" "Gender";
