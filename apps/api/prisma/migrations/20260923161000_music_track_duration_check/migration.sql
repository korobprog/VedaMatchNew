-- VED-310: длительность записи сверяется с файлом целиком. При загрузке
-- читается первый мегабайт, и у части записей `durationSeconds` — оценка по
-- размеру и битрейту, которая у VBR-файла расходится с настоящей длиной в
-- разы (пятиминутный трек показывался как «больше получаса», VED-165).
-- `NULL` значит «ещё не сверена»: фоновая стадия подбирает такие записи по
-- одной, дочитывает файл и ставит точное число. Колонка без значения по
-- умолчанию — весь существующий каталог встаёт в очередь на пересчёт.
-- Написано руками (см. CLAUDE.md).

-- AlterTable
ALTER TABLE "public"."MusicTrack" ADD COLUMN "durationCheckedAt" TIMESTAMP(3);

-- CreateIndex
CREATE INDEX "MusicTrack_durationCheckedAt_idx" ON "public"."MusicTrack"("durationCheckedAt");
