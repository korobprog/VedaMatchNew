-- Уведомления о задаче, с которой человек сам только что поработал, уходят
-- из ленты (VED-522): «задачи, которые ты делаешь, не должны вылазить у тебя
-- же в уведомлениях сразу после того, как ты внёс изменения». В «Истории»
-- строка остаётся. Новая новость о задаче от другого человека колонку
-- сбрасывает, и строка возвращается в ленту.
ALTER TABLE "NotificationItem" ADD COLUMN "feedHiddenAt" TIMESTAMP(3);
