-- Отметка о правке поста блог-ленты (VED-321).
-- Отдельная колонка, а не `updatedAt`: тот меняется от любой записи в строку
-- (закрепление, срок в ленте, счётчик репостов), и отметка «изменено»
-- появлялась бы у поста, которого автор не трогал.
ALTER TABLE "BlogPost" ADD COLUMN "editedAt" TIMESTAMP(3);
