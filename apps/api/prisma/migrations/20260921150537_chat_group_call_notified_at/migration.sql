-- Когда комната в последний раз будила беседу уведомлением «идёт звонок».
-- Отметка в строке, а не счётчик в памяти процесса: API работает не в одном
-- экземпляре, и условный UPDATE по этому полю заодно решает гонку — рассылает
-- тот, кто первым его переписал.
ALTER TABLE "public"."ChatGroupCall" ADD COLUMN "notifiedAt" TIMESTAMP(3);
