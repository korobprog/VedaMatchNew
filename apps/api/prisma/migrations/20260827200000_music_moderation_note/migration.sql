-- Причина решения модератора у записи. У загрузки её держать нельзя: снять с
-- публикации могут и то, что висело в каталоге месяц, когда строки загрузки
-- уже нет.
ALTER TABLE "public"."MusicTrack" ADD COLUMN "moderationNote" TEXT;
