-- Номер дорожки из тегов доставленного файла.
--
-- Нужен разбору архива: позиции заводятся до чтения тегов, а порядок
-- альбома известен только после него. Колонка у позиции, а не у трека —
-- в каталоге порядок задаёт подборка, и трек о своём номере не спрашивают.
ALTER TABLE "public"."MusicIngestItem" ADD COLUMN "trackNumber" INTEGER;
