-- Статус участника: короткая строка рядом с именем.
--
-- Портальное поле, как и рассказ о себе: показывается всем сервисам сразу.
-- В московский контур не уходит — оно не идентифицирует человека, граница
-- состава контура записана в personal-fields.ts.
--
-- Написано вручную, см. docs/prisma-raw-sql-objects.md.

ALTER TABLE "public"."User" ADD COLUMN "statusLine" TEXT;
