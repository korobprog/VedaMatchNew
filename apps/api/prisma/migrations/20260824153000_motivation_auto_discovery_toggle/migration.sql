-- Ежедневный автоподбор цитат воркером ИИ отключается по умолчанию:
-- редакция решила подбирать цитаты сама, очередь на проверку успела
-- вырасти без присмотра. Ручная кнопка «Подготовить цитаты на сегодня» в
-- админке — отдельный путь запуска, этой настройкой не затрагивается.

-- AlterTable
ALTER TABLE "MotivationSettings"
ADD COLUMN "autoQuoteDiscoveryEnabled" BOOLEAN NOT NULL DEFAULT false;
