-- Согласие показываться на общей карте «Общения».
--
-- Выключено по умолчанию осознанно: карта справочника — инструмент поиска, а
-- общая карта портала это другой уровень огласки. Заводить на неё людей молча
-- нельзя, даже если город у них и так открыт в карточке.
--
-- Сама метка ставится в центр города, а не по адресу, и людей на ней считают,
-- а не перечисляют — см. `PeopleService.mapCities`.
ALTER TABLE "ContactsProfile"
  ADD COLUMN "showOnMap" BOOLEAN NOT NULL DEFAULT false;
