# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

example

# generated native folders
/ios
/android

# Сборочные артефакты локальных Expo-модулей (modules/*/android — свой
# исходный код, но его Gradle-`build/`/`.cxx` — то же самое, что /android
# выше, просто на уровень глубже).
modules/*/android/build/
modules/*/android/.cxx/

# Firebase config is copied in from outside the repo
google-services.json

# Веб-сборка ios.vedamatch.com (pnpm export:web)
dist-web/
dist-web-b/
