/**
 * Заглушка секции «Проверить обновление» для сборки витрины
 * (`APP_CHANNEL=store`, VED-207). Подставляется резолвером Metro вместо
 * `@/components/self-update/self-update-section` — см. `resolve.cjs`.
 *
 * Сигнатура совпадает с настоящей секцией, но экран её всё равно не рендерит:
 * `capabilities.selfUpdate` на канале `store` равен `false`. Заглушка нужна не
 * ради поведения, а ради бандла — чтобы вместе с настоящим компонентом из
 * графа модулей ушло всё, что за ним тянется: загрузчик файла обновления,
 * системный установщик и адрес раздачи.
 *
 * Файл сам попадает в бандл витрины, поэтому в нём нет ни одной строки из
 * запрещённого списка `src/config/store-safety.ts` — иначе проверка бандла
 * ругалась бы на собственную заглушку.
 */
export function SelfUpdateSection() {
  return null;
}
