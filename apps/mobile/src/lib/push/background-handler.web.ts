/**
 * Веб-сборка: фонового приёмника FCM нет — `@react-native-firebase/messaging`
 * в браузере не работает. Уведомления веб-версии идут другим каналом.
 * Файл существует, чтобы `index.js` импортировал его вместо нативного
 * `background-handler.ts` (Metro выбирает `.web.ts` для платформы web).
 */
export {};
