export { ServiceIcon } from './service-icons-impl';

/**
 * На телефоне — прямой реэкспорт: и так одна сборка на всё приложение,
 * откладывать нечего. Веб-версия — `service-icons.web.tsx` рядом: та же
 * иллюстрация весит ~33 КБ несжатого JS (`react-native-svg`-разметка на
 * 13 сервисов) и нужна только на вкладке «Сервисы» после входа, а не на
 * экране входа — там она грузится отдельным чанком по требованию.
 */
