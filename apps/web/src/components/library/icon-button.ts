/**
 * Кнопка-значок в ряду действий рубрики (VED-511): «Фильтры»,
 * «Редактировать», «Упорядочить» — квадрат 44×44 рядом с «Добавить».
 * Подпись у каждой остаётся — в `aria-label` и подсказке `title`.
 */
export const LIBRARY_ICON_BUTTON =
  "inline-flex size-11 shrink-0 items-center justify-center rounded-xl border transition-colors";
