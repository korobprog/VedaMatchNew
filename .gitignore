node_modules/
dist/
.next/
.turbo/
.env
.env.*
!.env.example
*.log
coverage/

# Скиллы агентов, поставленные локально: их кладёт инструментарий под
# конкретную сессию, а не человек под задачу проекта. Рядом уже лежит
# `.superpowers/` по той же причине. Правило точечное: `.claude/launch.json`
# и `.claude/settings.json` — рабочие конфиги репозитория и отслеживаются.
.agents/
.claude/skills/
# Личные настройки сессии: туда же попадают токены доступа к внешним сервисам
# (Dokploy MCP читает DOKPLOY_API_KEY из окружения). В репозиторий не едет.
.claude/settings.local.json
skills-lock.json
.DS_Store
apps/api/scripts/test-motivation-ai-output.png
.superpowers/

# Локальные дампы БД (docker-compose backup)
backups/

# Исходники холста Claude Design. Мокапы публикуются артефактом, а холст —
# рабочий черновик на мегабайты с вшитыми картинками; в репозитории ему
# делать нечего.
.design/

# Сгенерированный клиент московской базы (prisma/ru/schema.prisma).
apps/api/node_modules/@vedamatch/ru-client/
