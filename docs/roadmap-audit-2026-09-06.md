# Список тестировщика от 2026-09-06 — сверка и слияние

Пункты с формулировками и решениями — в
[tester-feedback-2026-09-06.md](tester-feedback-2026-09-06.md). Здесь — как
считалась готовность и что было слито. Большой список из 81 пункта —
[roadmap-audit-2026-09-05.md](roadmap-audit-2026-09-05.md).

## Итог: 18 из 18

| Раздел | Пунктов | Готово |
| --- | --- | --- |
| Плеер | 2 | 2 |
| Чат | 3 | 3 |
| Панель горячих кнопок | 1 | 1 |
| Вдохновение | 12 | 12 |
| **Всего** | **18** | **18** |

Как это выглядело по ходу:

| Момент | Готово | % |
| --- | --- | --- |
| Сверка по одной текущей ветке | 3,5 | 19 % |
| Сверка по всем веткам | 12 | 67 % |
| В `main` до слияния | 0 | 0 % |
| После слияния и доработки | 18 | 100 % |

Урок отсюда простой и стоит того, чтобы его записать: **сверять готовность по
одной ветке бессмысленно.** Первая сверка насчитала 19 % там, где сделано было
67 %, — просто потому, что смотрела в текущий рабочий каталог. И ровно так же
бессмысленно радоваться 67 %: пока ветки не слиты, для человека на проде
готовность равна нулю.

## Что слито

Девять веток, по одной, с прогоном тестов после каждой:

| Ветка | Пункты |
| --- | --- |
| `fix/music-list-play` | 1, 2 |
| `fix/chat-unread-badge` | 3 |
| `feat/quick-calendar-link` | 4 |
| `fix/motivation-hide-text` | 5 |
| `fix/motivation-nav-and-categories` | 6, 8 |
| `feat/motivation-background-audio` | 7 |
| `fix/chat-reply-jump` | 16 |
| `feat/portal-assistant` | 17 |
| `feat/devotee-lineage` | 18, админское удаление к пункту 10 |

Поверх — один коммит с оставшимися пунктами 9–15.

Не тронуто по просьбе: `feat/chat-moments` — над ней работают сейчас.

Три вещи, которые стоит помнить о самом слиянии:

1. **`fix/music-list-play` отросла не от `main`, а от `chore/domain-com`.**
   Слить её целиком значило бы протащить в `main` переезд на vedamatch.com
   двенадцатью коммитами. Взят один нужный коммит.
2. **Пересечения.** `reels-feed.tsx` правили три ветки, `chat-room.tsx` и
   `chat-message.tsx` — две, `quick-actions.ts` — три. Автослияние справилось
   везде, кроме `packages/shared/src/index.ts`, где две ветки добавили свои
   экспорты в одну строку.
3. **После слияния обязателен `pnpm --filter @vedamatch/shared build` и
   `prisma generate`.** Без первого падают 55 веб-тестов, без второго — 44
   тестов API, и оба раза причина не в коде, а в устаревшем сгенерированном.
   Полдня можно потерять, разбирая «регрессию», которой нет.

## Состояние тестов

- Веб: 1703 из 1703.
- API: 3455 из 3456. Единственный красный —
  `gitabase-content.service.spec.ts`: на macOS временный каталог отдаётся как
  `/private/var/...`, а тест ждёт `/var/...`. Файл не менялся с июля, к этим
  веткам отношения не имеет.
- `pnpm lint` красный и до слияния: правило про `setState` в эффекте задевает
  файлы, которых никто из этих веток не касался (`member-counter.tsx`,
  `notification-settings.tsx`, `service-grid.tsx`). Отдельная задача.

## Чего в этом списке нет

Пункт 15 закрыт с оговоркой, которую надо держать в голове: кнопка
«Комментарий» появляется только у публикаций, собранных из читалки, — только у
них известна глава. Чтобы она была у всех шлок, редакционным публикациям
нужно проставить слаги главы отдельной задачей. Это работа по данным, не по
коду.
